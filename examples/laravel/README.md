# Пример использования с Laravel проектом

Этот пример показывает, как использовать AI Development Partner для разработки Laravel приложения.

## Сценарий: Создание API для интернет-магазина

### Начальная настройка промпта

Обновите секцию `Контекст Проекта` в промпте:

```yaml
### Технический Стек
- **OS:** Ubuntu 22.04
- **Web Server:** Nginx
- **Backend:** PHP 8.2, Laravel 11.x
- **Database:** MySQL 8.0
- **Admin Panel:** Filament
- **API Auth:** Laravel Sanctum
- **API Docs:** Swagger (L5-Swagger)
- **Queue:** Redis
- **Cache:** Redis
- **Storage:** AWS S3

### Стратегические Цели
- **Автоматизация:** Заменить 2 backend разработчиков
- **Бизнес-модель:** E-commerce platform
- **Финансовая цель:** 1М₽/мес выручки
- **Метрики:** Средний чек 5000₽, конверсия 3%
```

---

## Пример 1: Создание CRUD для Products

### Команда:
```
Создай CRUD для Products со следующими полями:
- title (string, required)
- description (text, optional)
- price (decimal, required)
- stock_quantity (integer, required)
- category_id (foreign key to categories)
- is_active (boolean, default true)

Также нужны:
- Валидация
- API Resource для трансформации
- Feature тесты
- Swagger документация
```

### Что генерирует AI:

1. **Команды для терминала:**
```bash
php artisan make:model Product -mf
php artisan make:controller Api/ProductController --api --model=Product
php artisan make:resource ProductResource
php artisan make:request StoreProductRequest
php artisan make:request UpdateProductRequest
php artisan make:test Feature/ProductControllerTest
```

2. **Модель** (`app/Models/Product.php`)
3. **Миграция** (`database/migrations/xxxx_create_products_table.php`)
4. **Контроллер** (`app/Http/Controllers/Api/ProductController.php`)
5. **Requests** для валидации
6. **API Resource** для трансформации ответов
7. **Роуты** в `routes/api.php`
8. **Тесты**
9. **Swagger аннотации**

---

## Пример 2: Добавление функционала корзины

### Шаг 1: Формируем ТЗ

```
/tz для функционала корзины покупок

Требования:
- Пользователь может добавлять товары в корзину
- Изменять количество
- Удалять товары
- Видеть общую сумму
- Корзина сохраняется между сессиями
- Проверка наличия товара на складе
```

### AI генерирует ТЗ:

```markdown
🎯 ТЕХНИЧЕСКОЕ ЗАДАНИЕ: Корзина покупок

## 1. Описание
Реализация функционала корзины для авторизованных пользователей...

## 2. Уточняющие вопросы
❓ Нужна ли корзина для неавторизованных (через сессию)?
❓ Максимальное количество товара в корзине?
❓ Как обрабатывать изменение цены товара после добавления в корзину?

## 3. Структура данных
- Таблица `cart_items`:
  - id
  - user_id (FK)
  - product_id (FK)
  - quantity
  - price_snapshot (цена на момент добавления)
  - created_at, updated_at

## 4. API Endpoints
POST /api/cart/items - Добавить товар
PUT /api/cart/items/{id} - Изменить количество
DELETE /api/cart/items/{id} - Удалить товар
GET /api/cart - Получить корзину с итоговой суммой
DELETE /api/cart - Очистить корзину

## 5. Бизнес-логика
- При добавлении проверять наличие на складе
- При изменении количества также проверять склад
- Автоматически удалять товар, если его нет в наличии
- Считать итоговую сумму с учетом price_snapshot

## 6. Риски
⚠️ Race condition при одновременном добавлении товара несколькими пользователями
⚠️ Устаревшие цены в корзине
⚠️ Товары, которых больше нет в наличии

## 7. Оценка
- Сложность: 6/10
- Время: 8 часов (с тестами)
- Стоимость: 24 000₽ (при ставке 3000₽/час)
```

### Шаг 2: Согласование и реализация

```
Ты: Отлично, давай реализуем. Начни с миграции и модели

AI: [Генерирует код]

Ты: Теперь API контроллер

AI: [Генерирует CartController с логикой]
```

---

## Пример 3: Интеграция платежной системы

### Команда:
```
/tz для интеграции оплаты через Stripe

Нужно:
- Создание платежного намерения (Payment Intent)
- Webhook для обработки успешной оплаты
- Обновление статуса заказа
- Логирование всех платежных операций
```

### AI генерирует план интеграции:

1. Установка пакета
2. Конфигурация
3. Создание сервиса `PaymentService`
4. Контроллер для платежей
5. Webhook endpoint
6. Middleware для верификации Stripe signatures
7. Event/Listener для обработки успешного платежа
8. Тесты с моками Stripe API

---

## Пример 4: Оптимизация производительности

### Команда:
```
У нас проблема с производительностью на странице списка товаров.
При загрузке 100 товаров выполняется 300+ SQL запросов.

Вот код контроллера:
[вставить код]
```

### AI анализирует и предлагает:

```markdown
🔍 АНАЛИЗ ПРОИЗВОДИТЕЛЬНОСТИ

## Проблемы:
1. ❌ N+1 проблема при загрузке категорий
2. ❌ N+1 проблема при загрузке изображений
3. ❌ Отсутствует пагинация
4. ❌ Нет кэширования

## Решение:

### 1. Eager Loading
\`\`\`php
Product::with(['category', 'images'])
    ->paginate(20);
\`\`\`

### 2. Добавить индексы
[Миграция с индексами]

### 3. Кэширование
\`\`\`php
Cache::remember('products.page.' . $page, 3600, function () {
    return Product::with(['category', 'images'])
        ->paginate(20);
});
\`\`\`

### 4. Результат:
- Запросов: 300+ → 3
- Время загрузки: 2.5s → 0.15s
- Нагрузка на БД: ↓95%
```

---

## Пример 5: Code Review

### Команда:
```
Проведи code review этого контроллера:

[вставить код OrderController]
```

### AI проводит анализ:

```markdown
📋 CODE REVIEW: OrderController

## ✅ Хорошо:
- Используется FormRequest для валидации
- Есть транзакции для создания заказа
- Правильная обработка ошибок

## ⚠️ Требует улучшения:

### 1. Нарушение Single Responsibility
Контроллер делает слишком много:
- Создание заказа
- Отправка email
- Обновление склада
- Создание платежа

**Рекомендация:** Вынести логику в Service class

### 2. Отсутствие Events
При создании заказа нет событий для других частей системы.

**Рекомендация:**
\`\`\`php
event(new OrderCreated($order));
\`\`\`

### 3. Жестко заданные магические числа
\`\`\`php
if ($total > 5000) { // Что это за число?
\`\`\`

**Рекомендация:** Использовать config:
\`\`\`php
if ($total > config('shop.free_shipping_threshold')) {
\`\`\```

### 4. Отсутствие тестов
Нет Feature тестов для проверки флоу создания заказа.

**Рекомендация:** [Генерирует тесты]

## 🔧 Рефакторинг:
[Предлагает улучшенную версию кода]
```

---

## Пример 6: Генерация тестов

### Команда:
```
Создай полный набор Feature тестов для ProductController

Нужно покрыть:
- CRUD операции
- Валидацию
- Авторизацию (только админы могут создавать/редактировать)
- Edge cases (несуществующий продукт, неверные данные)
```

### AI генерирует:

```php
<?php

namespace Tests\Feature;

use Tests\TestCase;
use App\Models\User;
use App\Models\Product;
use Illuminate\Foundation\Testing\RefreshDatabase;

class ProductControllerTest extends TestCase
{
    use RefreshDatabase;

    /** @test */
    public function guest_cannot_create_product()
    {
        $response = $this->postJson('/api/products', [
            'title' => 'Test Product',
            'price' => 100,
        ]);

        $response->assertUnauthorized();
    }

    /** @test */
    public function admin_can_create_product()
    {
        $admin = User::factory()->admin()->create();

        $response = $this->actingAs($admin, 'sanctum')
            ->postJson('/api/products', [
                'title' => 'Test Product',
                'description' => 'Test Description',
                'price' => 100.50,
                'stock_quantity' => 10,
                'is_active' => true,
            ]);

        $response->assertCreated()
            ->assertJsonStructure([
                'data' => [
                    'id',
                    'title',
                    'price',
                    'created_at',
                ],
            ]);

        $this->assertDatabaseHas('products', [
            'title' => 'Test Product',
            'price' => 100.50,
        ]);
    }

    // ... остальные тесты
}
```

---

## Пример 7: Разбивка на спринты

### Команда:
```
/sprints для создания интернет-магазина с нуля

Функционал:
- Каталог товаров с фильтрами и поиском
- Корзина
- Оформление заказа
- Интеграция с платежной системой
- Личный кабинет пользователя
- Админ панель для управления товарами и заказами
```

### AI генерирует план спринтов:

```markdown
🗓️ ПЛАН СПРИНТОВ (Интернет-магазин)

## Спринт 1 (40 часов)
### Цель: Базовая инфраструктура и каталог

**Задачи:**
1. [8h] Настройка проекта (Laravel, БД, миграции)
2. [12h] Модели и миграции (Products, Categories, Users)
3. [10h] API для каталога товаров (CRUD, фильтры, поиск)
4. [6h] Админ панель (Filament) - управление товарами
5. [4h] Тесты и документация

**Результат:** Работающий каталог с админкой

---

## Спринт 2 (32 часа)
### Цель: Корзина и авторизация

**Задачи:**
1. [8h] Авторизация (Sanctum, регистрация, логин)
2. [12h] Корзина (добавление, изменение, удаление)
3. [8h] API для работы с корзиной
4. [4h] Тесты

**Результат:** Пользователи могут добавлять товары в корзину

---

## Спринт 3 (40 часов)
### Цель: Оформление заказа

**Задачи:**
1. [10h] Модель Order, OrderItem
2. [12h] Процесс оформления заказа (создание, валидация)
3. [8h] Интеграция с Stripe (Payment Intent)
4. [6h] Webhook для обработки платежей
5. [4h] Тесты

**Результат:** Полный цикл заказа с оплатой

---

## Спринт 4 (24 часа)
### Цель: Личный кабинет и полировка

**Задачи:**
1. [10h] Личный кабинет (история заказов, профиль)
2. [8h] Email уведомления (заказ создан, оплачен, отправлен)
3. [6h] Финальное тестирование и баг-фиксы

**Результат:** Production-ready интернет-магазин

---

## Итого:
- **Время:** 136 часов
- **С запасом 20%:** 163 часа
- **Стоимость (3000₽/час):** 489 000₽
- **Срок:** ~4 недели (1 разработчик)
```

---

## Tips для работы с Laravel

1. **Используйте Feature тесты** - AI отлично их генерирует
2. **Просите Swagger документацию** - для каждого API endpoint
3. **Декомпозиция** - большие задачи разбивайте через `/tz` и `/sprints`
4. **Code Review** - используйте AI для проверки своего кода
5. **Оптимизация** - при проблемах с производительностью спрашивайте совета

## Дополнительные ресурсы

- [Laravel Documentation](https://laravel.com/docs)
- [Laravel Best Practices](https://github.com/alexeymezenin/laravel-best-practices)
- [AI Development Partner Docs](../../docs/)
